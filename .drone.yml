workspace:
  base: /go
  path: src/Seeds

pipeline:
  build:
    image: golang:latest
    environment:
      - GOPATH=/go
    commands:
      - go get -u github.com/golang/dep/cmd/dep
      - dep ensure
      - go build -o seeds
